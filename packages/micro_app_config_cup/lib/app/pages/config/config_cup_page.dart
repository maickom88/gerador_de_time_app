import 'package:flutter/material.dart';

class ConfigCupPage extends StatelessWidget {
  const ConfigCupPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
